apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-relay
  template:
    metadata:
      labels:
        app: order-relay
    spec:
      containers:
        - name: order-relay
          image: ticket-box-backend-order-relay:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          args: ["npx", "tsx", "src/daemons/outbox.relay.ts"]
          envFrom:
            - secretRef:
                name: order-service-env
          env:
            - name: SERVICE_NAME
              value: "order-relay"
            - name: PORT
              value: "3000"
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: PAYMENT_SERVICE_URL
              value: "http://payment-service:3000"
            - name: CONCERT_SERVICE_URL
              value: "http://concert-service:3000"
            - name: USER_SERVICE_URL
              value: "http://user-service:3000"

---
apiVersion: v1
kind: Service
metadata:
  name: order-relay
spec:
  type: ClusterIP
  selector:
    app: order-relay
  ports:
    - port: 3000
      targetPort: 3000
