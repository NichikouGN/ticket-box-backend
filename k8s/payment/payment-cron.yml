apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-cron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-cron
  template:
    metadata:
      labels:
        app: payment-cron
    spec:
      containers:
        - name: payment-cron
          image: ticket-box-backend-payment-cron:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          args: ["npx", "tsx", "src/jobs/outboxCron.job.ts"]
          envFrom:
            - secretRef:
                name: payment-service-env
          env:
            - name: SERVICE_NAME
              value: "payment-cron"
            - name: PORT
              value: "3000"
            - name: REDIS_URL
              value: "redis://redis:6379"

---
apiVersion: v1
kind: Service
metadata:
  name: payment-cron
spec:
  type: ClusterIP
  selector:
    app: payment-cron
  ports:
    - port: 3000
      targetPort: 3000
