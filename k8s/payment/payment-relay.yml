apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-relay
  template:
    metadata:
      labels:
        app: payment-relay
    spec:
      containers:
        - name: payment-relay
          image: ticket-box-backend-payment-relay:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          args: ["npx", "tsx", "src/daemons/outbox.relay.ts"]
          envFrom:
            - secretRef:
                name: payment-service-env
          env:
            - name: SERVICE_NAME
              value: "payment-relay"
            - name: PORT
              value: "3000"
            - name: REDIS_URL
              value: "redis://redis:6379"

---
apiVersion: v1
kind: Service
metadata:
  name: payment-relay
spec:
  type: ClusterIP
  selector:
    app: payment-relay
  ports:
    - port: 3000
      targetPort: 3000
