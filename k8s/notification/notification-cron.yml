apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-cron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-cron
  template:
    metadata:
      labels:
        app: notification-cron
    spec:
      containers:
        - name: notification-cron
          image: ticket-box-backend-notification-cron:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          args: ["npx", "tsx", "src/jobs/outboxCron.job.ts"]
          envFrom:
            - secretRef:
                name: notification-service-env
          env:
            - name: SERVICE_NAME
              value: "notification-cron"
            - name: PORT
              value: "3000"
            - name: REDIS_URL
              value: "redis://redis:6379"

---
apiVersion: v1
kind: Service
metadata:
  name: notification-cron
spec:
  type: ClusterIP
  selector:
    app: notification-cron
  ports:
    - port: 3000
      targetPort: 3000
