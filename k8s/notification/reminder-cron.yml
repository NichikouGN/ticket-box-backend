apiVersion: apps/v1
kind: Deployment
metadata:
  name: reminder-cron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reminder-cron
  template:
    metadata:
      labels:
        app: reminder-cron
    spec:
      containers:
        - name: reminder-cron
          image: ticket-box-backend-reminder-cron:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          args: ["npx", "tsx", "src/jobs/reminderCron.job.ts"]
          envFrom:
            - secretRef:
                name: notification-service-env
          env:
            - name: SERVICE_NAME
              value: "reminder-cron"
            - name: PORT
              value: "3000"
            - name: REDIS_URL
              value: "redis://redis:6379"

---
apiVersion: v1
kind: Service
metadata:
  name: reminder-cron
spec:
  type: ClusterIP
  selector:
    app: reminder-cron
  ports:
    - port: 3000
      targetPort: 3000
